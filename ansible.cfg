[defaults]
host_key_checking = False        # SSH 처음 접속 시 호스트 키 확인 비활성화
remote_user = ec2-user           # 기본 접속 계정

[ssh_connection]
ssh_args = -o ProxyCommand="sh -c \"aws ssm start-session --target %h --document-name AWS-StartSSHSession --parameters 'portNumber=%p'\""